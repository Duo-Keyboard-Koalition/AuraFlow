# PostgreSQL
POSTGRES_USER=auraflow
POSTGRES_PASSWORD=changeme

# MinIO (open source S3)
MINIO_ROOT_USER=auraflow
MINIO_ROOT_PASSWORD=changeme123

# Redis
REDIS_URL=redis://localhost:6379

# Database
DATABASE_URL=postgresql://auraflow:changeme@localhost:5432/auraflow

# AuraFlow bridge addon
NEXT_PUBLIC_AURAFLOW_BRIDGE_WS_URL=ws://127.0.0.1:8765
# Optional for local dev only.
# Production should use agent login challenge flow and JWT session tokens.
NEXT_PUBLIC_AURAFLOW_BRIDGE_TOKEN=

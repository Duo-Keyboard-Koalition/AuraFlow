# PostgreSQL
POSTGRES_USER=auraflow
POSTGRES_PASSWORD=changeme

# MinIO (open source S3)
MINIO_ROOT_USER=auraflow
MINIO_ROOT_PASSWORD=changeme123

# Redis
REDIS_URL=redis://localhost:6379

# Database
DATABASE_URL=postgresql://auraflow:changeme@localhost:5432/auraflow

# AuraFlow bridge addon
AURAFLOW_BRIDGE_HTTP_URL=http://127.0.0.1:8787
NEXT_PUBLIC_AURAFLOW_BRIDGE_WS_URL=ws://127.0.0.1:8787/v1/ws
# Optional in local dev if websocket auth is enabled on bridge.
NEXT_PUBLIC_AURAFLOW_BRIDGE_ACCESS_TOKEN=
